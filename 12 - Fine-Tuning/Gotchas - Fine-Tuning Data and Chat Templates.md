---
tags: [gotchas, domain/fine-tuning, level/core]
aliases: []
summary: "Seven data- and formatting-side pitfalls — template mismatches, masking bugs, tokenizer drift — that silently wreck a fine-tune."
---

## 1. Wrong chat template applied to the base model — the model never learns to stop
**Symptom:** after fine-tuning, the model runs on past where a turn should end, hallucinating a fake "user" turn and answering it, or otherwise produces garbled turn structure. **Cause:** every base model expects a specific set of role tags, delimiters, and special tokens — ChatML's `<|im_start|>`/`<|im_end|>` is not Llama-3's `<|start_header_id|>`/`<|eot_id|>` is not Mistral's `[INST]` format (mechanics owned by [[Concept - Chat Templates and Special Tokens]]). Training data rendered in the wrong template teaches the model a turn-boundary convention its tokenizer and base weights don't actually share. **Fix:** always render training examples through the target base model's own `apply_chat_template` (or its documented format), never a hardcoded string copied from a different model family's example script. **Detection:** this is the number-one silent failure in fine-tuning — always diff one rendered training example, token by token, against a known-good example from the base model's own model card before launching a full run.

## 2. Not masking the prompt out of the loss — the model learns to emit instructions
**Symptom:** at inference the fine-tuned model starts paraphrasing or repeating the system prompt / instructions back to the user instead of just answering. **Cause:** if the loss is computed over the full sequence (prompt plus completion) instead of only the completion tokens, cross-entropy pushes the model toward predicting the prompt tokens too — it partially learns "generate instructions," not just "follow them" (mechanics owned by [[Concept - Loss Masking and Sequence Packing]]). **Fix:** mask (set label to the ignore-index, e.g. -100) every token that isn't part of the target completion before computing loss. **Detection:** print the loss-mask array for a handful of samples and confirm the unmasked region starts exactly where the assistant's answer starts, not one token earlier or later.

## 3. Missing or duplicated EOS token — the model never stops, or stops early
**Symptom:** generation either runs to the max-token limit every time with no natural stop, or truncates answers mid-sentence. **Cause:** the end-of-sequence marker has to appear exactly once, in the right place, in every training example for the model to learn where to stop; a formatting script that forgets to append EOS, or accidentally appends it twice (once via a template helper, once manually), teaches an inconsistent stopping signal. **Fix:** verify every tokenized example ends with exactly one instance of the correct EOS token for that base model. **Detection:** decode the raw token ids (not the rendered text) of 20-30 samples and count EOS occurrences per example — it should always be exactly one, at the end.

## 4. Tokenizer mismatch between data prep and the model being trained
**Symptom:** training loss looks fine but generated output is subtly wrong — words fused together oddly, or specific tokens (especially near added special tokens, see [[Concept - Byte-Pair Encoding]]) come out corrupted. **Cause:** if the tokenizer used to pre-tokenize the dataset is a different revision than the one loaded at train time — added tokens, a changed vocab size, a re-trained merge table — token ids silently point to different subwords than the ones the model actually learned. This is invisible in the loss curve, because the model dutifully learns whatever ids it's given; the bug only surfaces at decode time. **Fix:** pin the tokenizer to the exact same revision/hash as the model checkpoint, load it once, and reuse that exact object for both data prep and training — never re-instantiate "the same" tokenizer by name in two separate places. **Detection:** hash-compare the tokenizer's vocab file (or `len(tokenizer)` plus a fixed test string's token ids) between the data-prep script and the training script.

## 5. Padding side and attention-mask mistakes — training on pad tokens
**Symptom:** short examples in a batch train noticeably worse than long ones, or the model develops a bias toward truncated-looking answers. **Cause:** causal LMs need padding excluded from the loss (and typically left-padding for generation, right-padding for training, depending on the framework) via the attention mask; if the mask isn't wired through correctly, the model computes gradient on pad tokens as if they were real content, and an all-one-length batching scheme can additionally bake in a length/format bias where every example looks truncated at the same point. **Fix:** confirm the attention mask (and label mask) excludes pad positions from both the forward computation's effective context and the loss, and mix example lengths within a batch rather than bucketing by exact same length. **Detection:** compute per-example loss and check it doesn't correlate suspiciously with sequence length; spot-check that pad-token positions have label set to the ignore-index.

## 6. Dataset too small or repetitive — memorization instead of generalization
**Symptom:** the model reproduces training examples near-verbatim on inputs that are only superficially different from training data; eval loss rises while train loss keeps falling. **Cause:** a small or low-diversity dataset gives the optimizer too few directions to generalize along — it's cheaper for gradient descent to memorize the finite example set than to learn the underlying pattern, especially across multiple epochs. **Fix:** widen sourcing diversity, reduce epoch count (one epoch is often enough on larger sets), and near-dedup the training pool itself, not just train-vs-eval. **Detection:** the classic signature is eval loss curving upward while train loss keeps dropping — that divergence point is roughly where training should have stopped or more data should have been added.

## 7. Eval-set contamination in training data — inflated metrics that don't hold in production
**Symptom:** the fine-tune's held-out eval score looks excellent, but production behavior on genuinely novel inputs is mediocre. **Cause:** if any training example is a near-duplicate of an eval example (or of a public benchmark being reported against), the "held-out" metric is partially measuring memorization, not generalization — see [[Concept - Benchmark Contamination]] for the general mechanism. **Fix:** near-dedup the training set against the eval set (and against any benchmark suite) before training, using the same technique as general dataset dedup. **Detection:** run a similarity search of every eval example against the full training set; any near-duplicate hit above a similarity threshold is contamination — this is exactly what [[Playbook - Evaluating a Fine-Tune]]'s ship-gate step is designed to catch before it ships.

## Connections
- [[Playbook - Preparing a Fine-Tuning Dataset]] — the procedure whose steps 5, 6, 8, and 9 exist specifically to avoid these seven gotchas.
- [[Playbook - Evaluating a Fine-Tune]] — detects gotchas 6 and 7 via the eval-loss/train-loss divergence and contamination checks.
- [[Gotchas - LoRA Fine-Tuning]] — the companion catalog for adapter-side (rather than data-side) pitfalls.
- [[Concept - Chat Templates and Special Tokens]] — owns the template mechanics behind gotcha 1.
- [[Concept - Loss Masking and Sequence Packing]] — owns the masking mechanics behind gotcha 2.
- [[Concept - Supervised Fine-Tuning (SFT)]] — the training process all seven gotchas ultimately corrupt.
- [[Concept - Benchmark Contamination]] — owns the general mechanism behind gotcha 7.
- [[Concept - Byte-Pair Encoding]] — owns the tokenizer mechanics behind gotchas 3 and 4.

## Sources
- These are field-accumulated pitfalls rather than results from a single paper; the primary mechanism citations live on the linked Concept notes above (chat templates, loss masking, contamination, and tokenization).
